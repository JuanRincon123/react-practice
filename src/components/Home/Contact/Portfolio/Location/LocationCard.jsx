import React from 'react'

const LocationCard = ({infoApi}) => {
    console.log(infoApi)
  return (
    <article>
      {/* <h1>{`#${infoApi?.id} - ${infoApi?.name}`}</h1> */}
    </article>
  )
}

export default LocationCard
